export interface Instrument {
    instrumentId: string,
    externalIdType: string,
    externalId: string,
    categoryId: string,
    instrumentDescription: string,
    maxQuantity: number,
    minQuantity: number
}
